services:
  - type: web
    name: blockchain-node
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python blockchain_node.py --port $PORT
    envVars:
      # PORT est défini automatiquement par Render, ne pas le définir ici
      - key: PYTHON_VERSION
        value: 3.11.5

