services:
  - type: web
    name: blockchain-node
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python blockchain_node.py --port $PORT
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.5

